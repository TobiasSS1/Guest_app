<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Guest_app"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:Guest_app.ViewModel"
    x:Class="Guest_app.MainPage"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.DataContext>
            <ViewModel:GuestViewModel/>
        </Grid.DataContext>
        <ListView x:Name="listView" HorizontalAlignment="Left" Height="239" Margin="150,355,0,0" VerticalAlignment="Top" Width="205" ItemsSource="{Binding GuestCollection, Mode=TwoWay}"/>
        <Button x:Name="button" Content="Opret gæst" HorizontalAlignment="Left" Margin="151,638,0,0" VerticalAlignment="Top"/>
        <Button x:Name="button1" Content="Slet gæst" HorizontalAlignment="Left" Margin="273,638,0,0" VerticalAlignment="Top"/>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="151,251,0,0" TextWrapping="Wrap" Text="Alle Gæster" VerticalAlignment="Top" FontSize="30"/>

    </Grid>
</Page>
